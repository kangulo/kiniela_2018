import { OnlypositivesDirective } from './onlypositives.directive';

describe('OnlypositivesDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlypositivesDirective();
    expect(directive).toBeTruthy();
  });
});
